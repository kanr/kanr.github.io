# Astro Starter

This is a fresh Astro 4.0 project set up for you!

## 🚀 Project Structure

```
/
├── public/           # Static assets (images, fonts, etc.)
├── src/
│   ├── components/   # Reusable Astro components
│   ├── layouts/      # Layout templates
│   ├── pages/        # Page routes (file-based routing)
│   └── styles/       # Global styles
├── astro.config.mjs  # Astro configuration
└── tsconfig.json     # TypeScript configuration
```

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command          | Action                                       |
| :--------------- | :------------------------------------------- |
| `npm install`    | Installs dependencies                        |
| `npm run dev`    | Starts local dev server at `localhost:3000` |
| `npm run build`  | Build your production site to `./dist/`     |
| `npm run preview`| Preview your build locally, before deploying |

## 📚 Learn More

- [Astro Documentation](https://docs.astro.build)
- [Astro Discord](https://astro.build/chat)

## ⚡ Quick Tips

- Pages in `src/pages/` automatically become routes
- Use Astro components (`.astro`) for server-rendered content
- Use layouts in `src/layouts/` to wrap your pages
- Add reusable components in `src/components/`

Happy coding! 🎉
